<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:application.MVVM.View.Auth"
	xmlns:Auth="clr-namespace:application.MVVM.ViewModel.Auth"
	x:Class="application.MVVM.View.Auth.ConfirmEmailView"
	mc:Ignorable="d"
	d:DesignHeight="450"
	d:DesignWidth="800">
	
	<UserControl.DataContext>
		<Auth:ConfirmPasswordViewModel />
	</UserControl.DataContext>

	<StackPanel
		VerticalAlignment="Center"
		HorizontalAlignment="Center">

		<!--<StackPanel>
			<TextBlock FontSize="40">Email confirmation</TextBlock>
		</StackPanel>-->
		
			<StackPanel>
				<TextBlock
					FontSize="20">
					Code for email@test.com
				</TextBlock>
				
				<StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
					<TextBox Style="{StaticResource ConfirmInput}"/>
					<TextBox Style="{StaticResource ConfirmInput}"/>
					<TextBox Style="{StaticResource ConfirmInput}"/>
					<TextBox Style="{StaticResource ConfirmInput}"/>
					<TextBox Style="{StaticResource ConfirmInput}"/>
					<TextBox Style="{StaticResource ConfirmInput}"/>
				</StackPanel>
				
				<!--<Button 
					Style="{StaticResource SendButton}" 
					Content="Check"/>-->
			</StackPanel>
		
		<!--<StackPanel HorizontalAlignment="Center">
			<Button
				Style="{StaticResource AuthAccountButton}">
				← Back
			</Button>
		</StackPanel>-->

	</StackPanel>

</UserControl>
