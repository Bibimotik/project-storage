<UserControl
	x:Class = "application.MVVM.View.Pages.AddStaffView"
	xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local = "clr-namespace:application.MVVM.View.Pages"
	xmlns:pages = "clr-namespace:application.MVVM.ViewModel.Pages"
	mc:Ignorable = "d">
	
	<StackPanel
		Width="300"
		Margin="10"
		Orientation="Vertical"
		VerticalAlignment="Center">
		
		<Label
			FontSize="45"
			Content="Add staff"/>
		<TextBlock
			Margin="0 0 0 8"
			Text="Email of user" />
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition
					Width="*" />
				<ColumnDefinition
					Width="80"/>
			</Grid.ColumnDefinitions>
			
			<TextBox
				x:Name="Email"
				Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
				Style="{StaticResource AuthInput}" />

			<Button
				Grid.Column="1"
				Margin="5 0 0 0"
				Content="GET"
				Command="{Binding GetUserCommand}"
				Style="{StaticResource SendButtonWhite}" />
		</Grid>
		
		<Label Visibility="Hidden" FontSize="1" Content="{Binding User_id}" />

		<Label Content="Name of user:" />
		<Label Content="{Binding Name}" />
		
		<ComboBox 
			Margin="0,10,0,0" 
			SelectedValue="{Binding Access, UpdateSourceTrigger=PropertyChanged}" 
			SelectedValuePath="Content">
			<ComboBoxItem IsSelected="True">Менеджер</ComboBoxItem>
			<ComboBoxItem>Аналитик</ComboBoxItem>
			<ComboBoxItem>Работник склада</ComboBoxItem>
		</ComboBox>
		
		<StackPanel Grid.Column="1">
			<Button
				Content="Add staff"
				Foreground="#ffffff"
				Margin="0,10,0,0"
				Width="300"
				Command="{Binding AddStaffCommand}"
				Style="{StaticResource SendButton}" />
			<Button
				Content="Back"
				Margin="0,10,0,0"
				Width="200"
				Height="25"
				Command="{Binding TriggerStaffCommand}"
				Style="{StaticResource SendButtonWhite}" />
		</StackPanel>
	</StackPanel>
</UserControl>