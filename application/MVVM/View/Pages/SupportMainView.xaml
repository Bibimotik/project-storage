<UserControl
	x:Class="application.MVVM.View.Pages.SupportMainView"
	xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local = "clr-namespace:application.MVVM.View.Pages"
	xmlns:Pages = "clr-namespace:application.MVVM.ViewModel.Pages"
	xmlns:converter = "clr-namespace:application.Utilities.Converter"
	mc:Ignorable = "d"
	d:DesignHeight = "300"
	d:DesignWidth = "300">
	
	<UserControl.Resources>
		<converter:BoolToVisibilityConverter
			x:Key="BoolToVisibilityConverter" />
	</UserControl.Resources>
	
	<StackPanel
		Width="500"
		Orientation="Vertical"
		VerticalAlignment="Center">
		
		<Label
			FontSize="45"
			Content="Report problem" />
		
		<StackPanel
			Margin="0 0 0 10"
			Orientation="Vertical">
			<TextBlock
				Margin="0 0 0 8"
				Text="Message" />
			<TextBox Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
			         MaxLength="600"
			         TextWrapping="Wrap" 
			         AcceptsReturn="True" 
			         Height="65" 
			         Style="{StaticResource AuthInput}"/>
			<TextBlock
				Visibility="{Binding IsInvalidMessage, Converter={StaticResource BoolToVisibilityConverter}}"
				Foreground="red"
				Text="Invalid Message..." />
		</StackPanel>
		
		<StackPanel
			Margin="0 0 0 10"
			Orientation="Vertical">
			<TextBlock
				Margin="0 0 0 8"
				Text="Attach Screenshot" />
			<Button
				Height="25"
				Content="{Binding SelectedFilePath}"
				Command="{Binding SelectFileCommand}" 
				Style="{StaticResource SendButtonWhite}" />
			<TextBlock
				Margin="5 0 0 0"
				Foreground="Gray"
				Text="{Binding SelectedFileNamesDisplay}" />
		</StackPanel>
		
		<Button 
			Content="Send to support" 
			Command="{Binding SendToSupportCommand}"
			Style="{StaticResource SendButton}"/>
		
	</StackPanel>
</UserControl>
