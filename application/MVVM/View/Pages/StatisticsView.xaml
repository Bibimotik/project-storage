<UserControl x:Class="application.MVVM.View.Pages.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Pages="clr-namespace:application.MVVM.ViewModel.Pages"
             xmlns:wpf="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d">

    <StackPanel Margin="20 0 10 0">
        <Label FontSize="45" Content="My statistics" Margin="0 0 0 10"/>

        <StackPanel Orientation="Horizontal">
            <Label Content="Start Date" VerticalAlignment="Center" Margin="0 10 0 0" />
            <DatePicker SelectedDate="{Binding StartDate}" Margin="0 10 0 0" />

            <Label Content="End Date" VerticalAlignment="Center" Margin="0 10 0 0" />
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0 10 0 0" />

            <Button Content="Load Statistics"
                    Command="{Binding LoadStatisticsCommand}"
                    Width="150"
                    Margin="10 10 0 0" 
                    Style="{StaticResource SendButton}"/>
        </StackPanel>
        <TextBlock Text="{Binding ValidationMessage}" Foreground="Red"/>

        <ScrollViewer Height="530">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="400"/>
                    <RowDefinition Height="400"/>
                    <RowDefinition Height="400"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="Black" Margin="0 10 0 0" BorderThickness="1" Padding="5" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Orders per Date" FontSize="12" FontWeight="Bold" Margin="0 0 0 2" HorizontalAlignment="Center" />
                        <wpf:CartesianChart Series="{Binding Series}" XAxes="{Binding XAxes}" Height="350" Margin="0 10 0 0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" BorderBrush="Black" Margin="0 10 0 0" BorderThickness="1" Padding="5" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Sales Products by Category" FontSize="12" FontWeight="Bold" Margin="0 0 0 2" HorizontalAlignment="Center" />
                        <wpf:PieChart Series="{Binding CategorySeries}" Height="350" Margin="0 10 0 0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" BorderBrush="Black" Margin="0 10 0 0" BorderThickness="1" Padding="5" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Product Sales Percentage" FontSize="12" FontWeight="Bold" Margin="0 0 0 2" HorizontalAlignment="Center" />
                        <wpf:CartesianChart Series="{Binding PercentageSeries}" XAxes="{Binding XAxes}" Height="350" Margin="0 10 0 0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </StackPanel>
</UserControl>
